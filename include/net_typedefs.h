#ifndef NET_TYPEDEFS_H
#define NET_TYPEDEFS_H

using EthernetHeader = struct ether_header;
using IPHeader = struct iphdr;
using ARPHeader = struct ether_arp;
using ICMPHeader = struct icmphdr;

#endif  // NET_TYPEDEFS_H
